---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: SERVENGINE-MODEL_TAG-inference
  labels:
    app: SERVENGINE-MODEL_TAG-inference
    model-type: "SERVENGINE"
    deployment-tag: "MODEL_TAG"
spec:
  replicas: REPLICAS_COUNT
  selector:
    matchLabels:
      app: SERVENGINE-MODEL_TAG-inference
  template:
    metadata:
      labels:
        app: SERVENGINE-MODEL_TAG-inference
        model-type: "SERVENGINE"
    spec:
      # 混合调度：优先HyperPod，资源不足时EKS NodeGroup，最后Karpenter自动扩展
      affinity:
        nodeAffinity:
          # 优先级调度
          preferredDuringSchedulingIgnoredDuringExecution:
          # 最高优先：HyperPod节点 (资源固定，性能稳定)
          - weight: 100
            preference:
              matchExpressions:
              - key: sagemaker.amazonaws.com/compute-type
                operator: In
                values: ["hyperpod"]
          # 次优先：EKS NodeGroup节点 (预付费资源)
          - weight: 90
            preference:
              matchExpressions:
              - key: alpha.eksctl.io/nodegroup-name
                operator: Exists
          # 第三优先：Karpenter节点 (按需自动扩展，成本优化)
          - weight: 80
            preference:
              matchExpressions:
              - key: karpenter.sh/capacity-type
                operator: Exists

          # 必须条件：实例类型匹配
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
HYBRID_NODE_SELECTOR_TERMS

      containers:
        - name: SERVENGINE
          image: DOCKER_IMAGE
          RESOURCES_SECTION
          command: ENTRY_CMD
          ports:
            - containerPort: PORT_NUMBER
              name: http
          volumeMounts:
            - name: persistent-storage-s3
              mountPath: /s3
              readOnly: true
            - name: local-cache
              mountPath: /root/.cache
            - name: shm
              mountPath: /dev/shm
          env:HF_TOKEN_ENV
            - name: TRANSFORMERS_CACHE
              value: "/root/.cache/huggingface"
          securityContext:
            capabilities:
              add:
                - IPC_LOCK
      volumes:
        - name: persistent-storage-s3
          persistentVolumeClaim:
            claimName: s3-claim
        - name: local-cache
          hostPath:
            path: /opt/dlami/nvme/.cache
            type: DirectoryOrCreate
        - name: shm
          emptyDir:
            medium: Memory
            sizeLimit: 2Gi
      tolerations:
        - key: "nvidia.com/gpu"
          operator: "Exists"
          effect: "NoSchedule"